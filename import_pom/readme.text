假设import_pom是我开发的一个组件,里面依赖了activity,为了避免别人引用我的这个组件的时候,
在自己的项目里面又自己引用了activity其它的版本号引起版本冲突,可以额外再创建一个pom包,
这个pom包专门用dependencyManagement来约束依赖方的各个版本号,不要跟自己出现冲突,
依赖方不是直接依赖那个工程自己的包,而是依赖于那个工程的pom类型的包,
然后再在dependency里面去声明自己要的依赖,此时依赖方引用其它版本的包就会报错